# GETWHATNOT — Offline / Local-Server Resilience Plan

> נשמר לפי בקשה. נחזור לזה. זה אפיון הכיוון שסוכם, לא קוד שנכתב עדיין.
> תאריך: 2026-07-07

## הבעיה שאנחנו פותרים
מה קורה כשהאינטרנט נופל במחסן — האם אפשר להמשיך לעבוד, ולסנכרן הכול כשהוא חוזר.
נפילות יכולות להימשך **עד כמה שעות**.

## מיפוי המכשירים (מהמשתמש)
- **אייפדים** = פיקרים. תפקידם: לראות הזמנות, למשוך/לאסוף מוצרים (מסך הפיקינג).
- **עמדות אריזה** = מחשבים פיזיים (PC). אורזים + מקליטים וידאו.

## מה חייב לעבוד offline (סוכם)
- סטטוס פיקינג/אריזה (סריקה, סימון פריט נמשך, חבילה נארזה).
- חיפוש/lookup בסריקה (לראות פרטי הזמנה offline).
- הקלטת וידאו.

## ההחלטה הארכיטקטונית (סוכם)
**מודל: שרת מקומי כ"מטמון + באפר" מול DB אמת אחד בענן.**

- יש **בסיס נתונים אמת אחד בענן** (Railway + R2). ייבוא CSV וצפייה באנליטיקה — מהענן או מקומי — זה תמיד אותו DB, אז אין קונפליקטים ואין כפילויות.
- **שרת מקומי** במחסן (mini PC על ה-LAN) מריץ עותק של האפליקציה + SQLite מקומי + תיקיית וידאו מקומית.
- **האייפדים והמחשבים תמיד מדברים עם השרת המקומי** (IP קבוע ב-LAN).
- כשיש אינטרנט: השרת המקומי מושך את נתוני השאו הפעיל מהענן (למטמון), ודוחף כתיבות לענן בזמן אמת.
- כשאין אינטרנט: מגיש מהמטמון המקומי + מכניס כל פעולה ל-**תור (outbox)**.
- כשהאינטרנט חוזר: מריק את התור לענן (סטטוסים + העלאת וידאו ל-R2). הכול **idempotent** (UUID לכל אירוע) — בלי כפילויות.
- **וידאו נשמר קודם על הדיסק של השרת המקומי** (אמין), לא על האייפד/מחשב — פותר את בעיית "שעות של וידאו שנתקע על אייפד".

### החלטות שאושרו
- סנכרון **חד-כיווני בעיקרו** (מקומי→ענן לפעולות; ענן→מקומי לייבוא/הגדרות). **לא** master-master דו-כיווני מלא.
- **ייבוא CSV נעשה רק כשיש אינטרנט** — המשתמש אישר שזה בסדר. (נתוני שאו שכבר יובאו נמצאים במטמון, אז פיקינג/אריזה ממשיכים offline.)
- מקור אמת אחד = הענן. השרת המקומי = עותק-עבודה + באפר.

## מה שאתה (הבעלים) מסדר — חומרה + רשת
- **Mini PC** תמידי (למשל Intel N100), ~$150-250, עם **SSD 500GB+** (יום וידאו = כמה ג'יגות עד שמתנקה אחרי העלאה).
- **IP קבוע** בראוטר (DHCP reservation), למשל `192.168.1.50`.
- כל האייפדים/מחשבים עם bookmark ל-`http://192.168.1.50` (או שם ידידותי כמו `warehouse.local`).
- WiFi יציב לאייפדים; רשת אחת (LAN) לכל המכשירים.

## מה שאני בונה — בשלבים
1. **מצב "node" מקומי** — הרצת האפליקציה על ה-mini PC עם זהות node ייחודית (env: `NODE_ROLE=local`, `NODE_ID`, `CLOUD_URL`).
2. **משיכה ענן→מקומי** — השרת המקומי מושך שאוים פעילים (shipments + shipment_items) + הגדרות כשיש אינטרנט, כדי שיהיו במטמון ל-offline.
3. **תור דחיפה מקומי→ענן** — טבלת `outbox(uuid, table, op, payload, created_at, synced)`. כל פעולה (pick toggle, pack complete, status, video) נכתבת מקומית מיד + נכנסת ל-outbox. worker דוחף לענן כשיש אינטרנט (+ העלאת וידאו ל-R2). idempotent לפי UUID.
4. **endpoint קליטה בענן** — `/api/sync/ingest` שמקבל אצווה של אירועים מה-node המקומי ומיישם אותם (idempotent).
5. **זיהוי offline בצד הלקוח (PWA)** — Service Worker לטעינת ה-app shell, באנר "מצב מקומי / אין אינטרנט", ו-retry אוטומטי. כך הצוות תמיד יודע איפה הוא עומד.

## נקודות פתוחות / להחליט כשנחזור
- האם השרת המקומי מריץ את אותו קוד בדיוק (branch/flag) או build נפרד.
- מנגנון גילוי online/offline בשרת המקומי (ping לענן / חיווי).
- retention של וידאו מקומי אחרי העלאה מוצלחת (מחיקה כדי לפנות דיסק).
- אבטחת ה-node המקומי מול הענן (מפתח/טוקן ל-node).
- מה קורה אם השרת המקומי נופל (single point of failure) — אולי fallback לענן כשיש אינטרנט.
- הרצה: Docker על ה-mini PC, או Python ישיר + service שמריץ אוטומטית בעליית המכשיר.

## איפה עצרנו
המשתמש ביקש לשמור את זה ולחזור אליו בהמשך. הצעד הבא כשנחזור: להתחיל משלב 1–2 (זהות node + טבלת outbox + endpoint קליטה + משיכת שאוים פעילים), או קודם לכתוב spec מלא של הזרימה.
